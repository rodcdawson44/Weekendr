int main()
{
  
  string line = getline(cin);
  string output;
  output.push_back(line[0]);
  
  for(int i=0; i<line.size(); i++){
    if(i !=line.size()-1){
      
      if(letter == ' '){
        string str = line[i+1];
        output += line[i+1]; 
      } 
    }
   
  }
  string str = "geeks for geeks"; 
  
    
  
}